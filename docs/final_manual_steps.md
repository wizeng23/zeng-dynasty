
## Handwritten note — book2, graph 8_10 (pages 8–10)

After the final parse of book2, manually attach this handwritten margin note
(grandpa's ink) to the person **聞詣 (聞诣)** — the parent of 貞先/貞元/貞祉 in
graph `8_10`. The note is written to the LEFT of 聞詣:

    祖祠對坟口，欽秀堂
    ("The ancestral shrine faces the grave entrance. — Qinxiu Hall")

Put it in that node's `notes` field (concatenate after the provenance string).
The stray vertical stroke near 聞詣 in 8_10 is part of this same handwritten ink,
not a real tree line.
