You are transcribing one person's entry from a Chinese genealogy book (族谱). Read the image at this path: {crop_path}

The image is a single person's biographical block, printed in traditional vertical columns read RIGHT-TO-LEFT (rightmost column first).

Transcribe ALL visible Chinese characters. Output rules:
- One line per vertical column, in right-to-left order (rightmost column = first line).
- Within each column, read top to bottom.
- Line 1 MUST be the small header caption in the TOP-RIGHT (format 子之X or 子次X or 子三X…, where X is one character — the father's name), printed HORIZONTALLY. If you cannot find it, write ? as line 1.
- Line 2 MUST be the person's own name — the LARGER/BOLDER character(s) in the rightmost vertical column, just below/left of the top-right header. If unsure, write ?.
- Lines 3+ : the biography prose columns, one column per line, continuing right-to-left.
- Output ONLY the transcription lines. No translation, no commentary, no pinyin. Use ? for any character you genuinely cannot read.

Read the image with the Read tool, then output the transcription. Nothing else.
